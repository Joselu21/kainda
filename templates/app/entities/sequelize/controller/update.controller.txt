async function update%%$MODEL_NAME_UPPERCASE$%%(req, res) {
    let transaction = await Models.%%$MODEL_NAME_UPPERCASE$%%.transaction();
    try {
        let container = { ...req.body, [Models.%%$MODEL_NAME_UPPERCASE$%%.modelId]: req.params.%%$MODEL_NAME_LOWERCASE$%%_id };
        const %%$MODEL_NAME_LOWERCASE$%% = await Models.%%$MODEL_NAME_UPPERCASE$%%.Controller.__update%%$MODEL_NAME_UPPERCASE$%%(container, {transaction});
        await transaction.commit();
        return res.status(200).json(%%$MODEL_NAME_LOWERCASE$%%.toJSON()); 
    } catch (error) {
        if(transaction) {
            await transaction.rollback();
        }
        ExceptionHandler(error, res);
    }
}

module.exports = {
    update%%$MODEL_NAME_UPPERCASE$%%
}