const assert = require("assert");
const supertest = require("supertest");

module.exports = function () 
{

    describe ("Update endpoint", function () 
    {
        it ("Update a __KAINDA__MODEL__UPPERCASE__ successfully (200)");

        it ("Update a __KAINDA__MODEL__UPPERCASE__ with invalid data (400)");

        it ("Update a __KAINDA__MODEL__UPPERCASE__ with invalid token (401)");

        it ("Update a __KAINDA__MODEL__UPPERCASE__ with no permission (403)");

        it ("Update a __KAINDA__MODEL__UPPERCASE__ with invalid id (404)");
    });

};