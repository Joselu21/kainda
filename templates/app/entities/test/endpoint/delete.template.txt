const assert = require('assert');
const supertest = require('supertest');

module.exports = function () {

    describe ('Delete endpoint', function () {
        it ('Delete a pruebaTransaction successfully (200)');

        it ('Delete a pruebaTransaction with invalid token (401)');

        it ('Delete a pruebaTransaction with no permission (403)');

        it ('Delete a pruebaTransaction with invalid id (404)');
    });

};