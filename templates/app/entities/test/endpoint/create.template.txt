const assert = require('assert');
const supertest = require('supertest');

module.exports = function () {

    describe ('Create endpoint', function () {
        it ('Create a pruebaTransaction successfully (200)');

        it ('Create a pruebaTransaction with invalid data (400)');

        it ('Create a pruebaTransaction with invalid token (401)');

        it ('Create a pruebaTransaction with no permission (403)');

        it ('Create a pruebaTransaction with conflicting data (409)');
    });

};